document.querySelector('.login-image img').addEventListener('contextmenu', function (event) {
    event.preventDefault();
});

document.querySelector('.logo img').addEventListener('contextmenu', function (event) {
    event.preventDefault();
});
